チェックボックス列 (BooleanField) の見出しに置く全選択チェックボックス。クリックすると全行の `FieldName` のフィールド値を一括で ON/OFF する。

- 列の `FieldName` に ON/OFF 対象の BooleanField を指定する (行側は通常どおり各行のチェックボックスが描画される)
- `Width: 40` 程度の固定幅・`CanResize: false`・`CanUserSort: false` にするのが定番
