<Controls:MetroWindow x:Class="Codeer.LowCode.Blazor.Extras.Designer.Setup.SetupResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="Setup Result" Height="560" Width="760" ResizeMode="CanResizeWithGrip">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBox x:Name="_textSummary" Grid.Row="0" IsReadOnly="True" BorderThickness="0"
             TextWrapping="Wrap" Background="Transparent" Margin="0,0,0,8"/>

    <TextBlock x:Name="_labelDdl" Grid.Row="1" Margin="0,0,0,4" FontWeight="Bold"/>

    <TextBox x:Name="_textDdl" Grid.Row="2" IsReadOnly="True" AcceptsReturn="True"
             FontFamily="Consolas" FontSize="13"
             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
      <Button x:Name="_buttonRun" Width="120" Margin="5" Click="RunClick"/>
      <Button x:Name="_buttonCopy" Width="100" Margin="5" Click="CopyClick"/>
      <Button x:Name="_buttonClose" Width="100" Margin="5" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Controls:MetroWindow>
